@model Tracker.Domain.CategoryType

<input
    name="name"
    required="required"
    value="@Model.Name"
    autofocus
    hx-patch="@Url.Action("Index", "Category", new { id = Model.Id })"
    hx-target="this"
    hx-swap="outerHTML"
    hx-trigger="blur delay:500ms"/>